{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing for the unbeatable minimax engine. Stryker mutates lib/ai.ts and reruns the Vitest suite; surviving mutants reveal logic the exhaustive 'never loses' proof does not actually pin down.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "reporters": ["html", "clear-text", "progress"],
  "coverageAnalysis": "perTest",
  "mutate": ["lib/ai.ts"],
  "vitest": {
    "configFile": "vitest.config.ts"
  },
  "thresholds": {
    "high": 90,
    "low": 80,
    "break": 80
  }
}
